<?xml version="1.0" encoding="utf-8"?>
<search>
  <entry>
    <title>Python 实现动态链接库效果</title>
    <url>/2025/05/21/pseudo-dll/</url>
    <content><![CDATA[<p>需求来源是做<code>OSM</code> 编辑项目中要用Python 对<code>.osm</code> 文件进行校验，但是嘞，我的Python 代码
是需要用Nuitka 打包成二进制的，而校验规则又可能会随时调整，每次增减规则都要重新打包的话非常浪费时间。因此，便想能否通过类似与调用<code>.dll</code> 一样来动态加载<code>.py</code> 文件（伪动态链接库）。</p>
<span id="more"></span>
<h2 id="前置要求"><a class="markdownIt-Anchor" href="#前置要求"></a> 前置要求</h2>
<ol>
<li>Nuitka 打包后的二进制文件要包含完整的Python 运行时；✅</li>
<li><code>.py</code> 文件要有统一的入口函数；✅</li>
<li><code>.py</code> 文件的依赖项要提前在二进制文件中<code>import</code>。✅</li>
</ol>
<h2 id="实现代码"><a class="markdownIt-Anchor" href="#实现代码"></a> 实现代码</h2>
<p>演示代码主要分为主文件<code>main.py</code> 和库文件<code>ddl.py</code>，二者在相同目录：</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="comment"># main.py</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">import</span> os</span><br><span class="line"><span class="keyword">import</span> uuid</span><br><span class="line"><span class="keyword">import</span> importlib.util</span><br><span class="line"></span><br><span class="line"><span class="comment">###  调用外部.py 前的准备工作 ###</span></span><br><span class="line"><span class="keyword">import</span> numpy <span class="keyword">as</span> np  <span class="comment"># 外部模块中需要用到的依赖项，需要提前导入准备</span></span><br><span class="line">module_path = os.path.abspath(<span class="string">&quot;dll.py&quot;</span>)  <span class="comment"># 获取目标.py 文件的绝对路径（此处可以读取该目录下的所有.py 文件，循环导入）</span></span><br><span class="line">module_name = <span class="string">f&quot;_dynamic_module_<span class="subst">&#123;uuid.uuid4().<span class="built_in">hex</span>&#125;</span>&quot;</span>  <span class="comment"># 生成随机的模块名</span></span><br><span class="line"><span class="comment"># 模块名作为模块身份的唯一标识，仅会被导入一次，通过随机化处理可以避免命名重复而无法导入</span></span><br><span class="line"></span><br><span class="line">spec = importlib.util.spec_from_file_location(module_name, module_path)  <span class="comment"># 创建一个加载说明（描述符）</span></span><br><span class="line">module = importlib.util.module_from_spec(spec)  <span class="comment"># 根据加载说明加载.py 文件为模块</span></span><br><span class="line">spec.loader.exec_module(module) <span class="comment"># 执行模块</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># 使用模块中的函数</span></span><br><span class="line">module.hello()  <span class="comment"># 有必要约定模块中的函数签名保持一致，否则可能会报错</span></span><br></pre></td></tr></table></figure>
<p>以下是库文件</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="comment"># dll.py</span></span><br><span class="line"><span class="keyword">import</span> numpy <span class="keyword">as</span> np  <span class="comment"># numpy 需要提前打包在二进制文件中</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">def</span> <span class="title function_">hello</span>():</span><br><span class="line">    <span class="built_in">print</span>(<span class="string">&quot;Hello from dll.py!&quot;</span>)</span><br></pre></td></tr></table></figure>
<p>通过<code>python -m nuitka --clang --standalone --onefile main.py</code> 即可打包成二进制文件，即便在没有安装Python 环境的机器上也可以运行了。</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">$ ./main.bin </span><br><span class="line">Hello from dll.py!</span><br></pre></td></tr></table></figure>
]]></content>
      <categories>
        <category>python</category>
      </categories>
      <tags>
        <tag>编程技巧</tag>
      </tags>
  </entry>
  <entry>
    <title>使用gettext 进行国际化</title>
    <url>/2025/05/15/gettext/</url>
    <content><![CDATA[<p>一般来说，使用<code>gettext</code> 为程序添加多语言支持时，需要遵循一些标准的步骤。另外就是，对于在<code>main()</code> 函数之前就初始化的变量需要特殊处理。
为此，特整理笔记备忘。</p>
<span id="more"></span>
<h2 id="最小可用示例"><a class="markdownIt-Anchor" href="#最小可用示例"></a> 最小可用示例</h2>
<p>该示例代码仅保证多语言功能可用：</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;iostream&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;locale.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;libintl.h&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="meta">#<span class="keyword">define</span> _(STRING) gettext(STRING)</span></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="type">int</span> <span class="title">main</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    <span class="built_in">setlocale</span>(LC_ALL, <span class="string">&quot;&quot;</span>);  <span class="comment">// 指定语言，例如en_US，zh_CN，zh_CN.UTF-8 等</span></span><br><span class="line">    <span class="built_in">bindtextdomain</span>(<span class="string">&quot;myapp&quot;</span>, <span class="string">&quot;./locale&quot;</span>); <span class="comment">// .locale 是语言包的路径，可以是相对路径也可以是绝对路径</span></span><br><span class="line">    <span class="built_in">textdomain</span>(<span class="string">&quot;myapp&quot;</span>);</span><br><span class="line">    <span class="comment">// 上面的myapp 可以改成自己喜欢的名字，但是文件路径需要遵循以下规则  </span></span><br><span class="line">    <span class="comment">// ./locale/&#123;zh_CN&#125;/LC_MESSAGES/&#123;myapp&#125;.mo  </span></span><br><span class="line">    <span class="comment">// 其中.mo 可以通过以下步骤生成</span></span><br><span class="line"></span><br><span class="line">    std::cout &lt;&lt; _(<span class="string">&quot;Hello, world!&quot;</span>) &lt;&lt; std::endl;</span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br><span class="line">```  </span><br><span class="line"></span><br><span class="line">### 提取、翻译   </span><br><span class="line">```bash  </span><br><span class="line"># 从main.cpp 中提取待翻译的内容</span><br><span class="line">xgettext --from-code=UTF<span class="number">-8</span> -k_ main.cpp -o messages.pot</span><br><span class="line"></span><br><span class="line"># 同时提取多个文件</span><br><span class="line">find . -name <span class="string">&quot;*.cpp&quot;</span> | xargs xgettext --from-code=UTF<span class="number">-8</span> -k_ -o messages.pot</span><br></pre></td></tr></table></figure>
<p>通过<a href="https://poedit.net/">poedit</a> 可以打开.pot 文件，并将翻译结果保存到相应的路径下。</p>
<h2 id="静态变量翻译"><a class="markdownIt-Anchor" href="#静态变量翻译"></a> 静态变量翻译</h2>
<p>上面内容不太重要，多试试就好了。但是静态变量的翻译就很有意思了。</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;iostream&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;locale.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;libintl.h&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="meta">#<span class="keyword">define</span> _(STRING) gettext(STRING)</span></span><br><span class="line"></span><br><span class="line"><span class="type">const</span> <span class="type">char</span> * bye = _(<span class="string">&quot;Goodbye!&quot;</span>);</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="type">int</span> <span class="title">main</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    <span class="built_in">setlocale</span>(LC_ALL, <span class="string">&quot;&quot;</span>);  <span class="comment">// 指定语言，例如en_US，zh_CN，zh_CN.UTF-8 等</span></span><br><span class="line">    <span class="built_in">bindtextdomain</span>(<span class="string">&quot;myapp&quot;</span>, <span class="string">&quot;./locale&quot;</span>); <span class="comment">// .locale 是语言包的路径，可以是相对路径也可以是绝对路径</span></span><br><span class="line">    <span class="built_in">textdomain</span>(<span class="string">&quot;myapp&quot;</span>);</span><br><span class="line"></span><br><span class="line">    std::cout &lt;&lt; _(<span class="string">&quot;Hello, world!&quot;</span>) &lt;&lt; std::endl;  <span class="comment">// 可以正常显示对应语言</span></span><br><span class="line">    std::cout &lt;&lt; bye &lt;&lt; std::endl;  <span class="comment">// 无法正常显示对应语言</span></span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>因为<code>bye</code> 是在<code>main()</code> 之前初始化的，所以在<code>main()</code> 执行时就获取不到对应的译文了。为此，可以封装一个函数：</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">define</span> _(STRING) _translate(STRING)</span></span><br><span class="line"><span class="type">void</span> _translate(<span class="type">const</span> <span class="type">char</span>* msg) &#123;</span><br><span class="line">    <span class="built_in">setlocale</span>(LC_ALL, <span class="string">&quot;&quot;</span>);  <span class="comment">// 指定语言，例如en_US，zh_CN，zh_CN.UTF-8 等</span></span><br><span class="line">    <span class="built_in">bindtextdomain</span>(<span class="string">&quot;myapp&quot;</span>, <span class="string">&quot;./locale&quot;</span>); <span class="comment">// .locale 是语言包的路径，可以是相对路径也可以是绝对路径</span></span><br><span class="line">    <span class="built_in">textdomain</span>(<span class="string">&quot;myapp&quot;</span>);</span><br><span class="line"></span><br><span class="line">    <span class="keyword">return</span> <span class="built_in">gettext</span>(msg);</span><br><span class="line">&#125;</span><br><span class="line">```  </span><br><span class="line"></span><br><span class="line">### 多语言初始化标识符</span><br><span class="line">每次运行`_()` 前都设置下语言就好了。但是这样会造成额外的开销，为此我们可以通过一个全局的标识符`gettext_initialized` 来表示语言是否初始化过了。</span><br><span class="line">```cpp</span><br><span class="line"><span class="meta">#<span class="keyword">define</span> _(STRING) _translate(STRING)</span></span><br><span class="line"></span><br><span class="line"><span class="type">bool</span> gettext_initialized = <span class="literal">false</span>;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">ensure_gettext_initialized</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    <span class="keyword">if</span> (!gettext_initialized) &#123;</span><br><span class="line">        <span class="built_in">setlocale</span>(LC_ALL, <span class="string">&quot;&quot;</span>);</span><br><span class="line">        <span class="built_in">bindtextdomain</span>(<span class="string">&quot;gmsh&quot;</span>, <span class="string">&quot;./locale&quot;</span>);</span><br><span class="line">        <span class="built_in">textdomain</span>(<span class="string">&quot;gmsh&quot;</span>);</span><br><span class="line">        gettext_initialized = <span class="literal">true</span>;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"><span class="type">const</span> <span class="type">char</span>* _translate(<span class="type">const</span> <span class="type">char</span>* msg) &#123;</span><br><span class="line">    <span class="built_in">ensure_gettext_initialized</span>();</span><br><span class="line">    <span class="keyword">return</span> <span class="built_in">gettext</span>(msg);</span><br><span class="line">&#125;</span><br><span class="line">```  </span><br><span class="line"></span><br><span class="line">### 多线程安全  </span><br><span class="line">其实上面的代码在大部分情况下已经可用了，但是多线程的情况下可能会有一丢丢的问题（但仔细想想其实无关紧要，最多是多初始化几次而已）。为此，可以添加线程锁，以下是完整代码（未作验证）：  </span><br><span class="line">```cpp</span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;iostream&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;locale.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;libintl.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;mutex&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="meta">#<span class="keyword">define</span> _(STRING) _translate(STRING)</span></span><br><span class="line">std::mutex gettext_mutex;</span><br><span class="line"><span class="type">bool</span> gettext_initialized = <span class="literal">false</span>;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">ensure_gettext_initialized</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    <span class="function">std::lock_guard&lt;std::mutex&gt; <span class="title">lock</span><span class="params">(gettext_mutex)</span></span>;</span><br><span class="line">    <span class="keyword">if</span> (!gettext_initialized) &#123;</span><br><span class="line">        <span class="built_in">setlocale</span>(LC_ALL, <span class="string">&quot;&quot;</span>);  <span class="comment">// 也可用export LC_MESSAGES=zh_CN.UTF-8 指定语言</span></span><br><span class="line">        <span class="built_in">bindtextdomain</span>(<span class="string">&quot;myapp&quot;</span>, <span class="string">&quot;./locale&quot;</span>);</span><br><span class="line">        <span class="built_in">textdomain</span>(<span class="string">&quot;myapp&quot;</span>);</span><br><span class="line">        gettext_initialized = <span class="literal">true</span>;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="type">const</span> <span class="type">char</span>* _translate(<span class="type">const</span> <span class="type">char</span>* msg) &#123;</span><br><span class="line">    <span class="built_in">ensure_gettext_initialized</span>();</span><br><span class="line">    <span class="keyword">return</span> <span class="built_in">gettext</span>(msg);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="type">const</span> <span class="type">char</span>* messages[] = &#123;</span><br><span class="line">    _(<span class="string">&quot;Hello&quot;</span>),</span><br><span class="line">    _(<span class="string">&quot;Goodbye!&quot;</span>)</span><br><span class="line">&#125;;</span><br><span class="line"><span class="type">const</span> <span class="type">char</span> * bye = _(<span class="string">&quot;Goodbye!&quot;</span>);</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="type">int</span> <span class="title">main</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    std::cout &lt;&lt; _(<span class="string">&quot;Hello, world!&quot;</span>) &lt;&lt; std::endl;</span><br><span class="line">    </span><br><span class="line">    std::cout &lt;&lt; bye &lt;&lt; std::endl; </span><br><span class="line">    <span class="keyword">for</span> (<span class="keyword">auto</span> key : messages) &#123;</span><br><span class="line">        std::cout &lt;&lt; key &lt;&lt; std::endl;  <span class="comment">// 运行时翻译</span></span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br><span class="line">```  </span><br><span class="line"></span><br><span class="line">该操作甚至可以翻译数组`messages[]` 中的字符串。😮</span><br><span class="line"></span><br><span class="line">## 其他  </span><br><span class="line">### 修改FLTK 支持字体  </span><br><span class="line">默认显示汉语乱码，可以修改`FlGui::<span class="built_in">FlGui</span>()` 构造函数，添加：</span><br><span class="line">```<span class="function">cpp</span></span><br><span class="line"><span class="function"><span class="title">FlGui::FlGui</span><span class="params">(<span class="type">int</span> argc, <span class="type">char</span> **argv, <span class="type">bool</span> quitShouldExit,</span></span></span><br><span class="line"><span class="params"><span class="function">             <span class="type">void</span> (*error_handler)(<span class="type">const</span> <span class="type">char</span> *fmt, ...))</span></span></span><br><span class="line"><span class="function">  : _quitShouldExit(quitShouldExit), lastContextWindow(<span class="number">0</span>)</span></span><br><span class="line"><span class="function">&#123;</span></span><br><span class="line">  Fl::<span class="built_in">set_font</span>(FL_HELVETICA, <span class="string">&quot;Noto Sans CJK SC&quot;</span>); <span class="comment">// 确保系统已安装该字体</span></span><br><span class="line"></span><br><span class="line">  <span class="comment">// ...</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h3 id="批量替换文件内容"><a class="markdownIt-Anchor" href="#批量替换文件内容"></a> 批量替换文件内容</h3>
<p>在vscode 中，使用<code>ctrl+shift+f</code> 可以全局查询，并按正则表达式替换内容：</p>
<figure class="highlight txt"><table><tr><td class="code"><pre><span class="line">Msg::Error\s*\(\s*&quot;((?:[^&quot;\\]|\\.)*)&quot;\s*\)</span><br><span class="line"></span><br><span class="line">匹配&quot;&quot; 中的字符串，支持转义字符。并替换为</span><br><span class="line"></span><br><span class="line">Msg::Error(_(&quot;$1&quot;))</span><br></pre></td></tr></table></figure>
<p>在<code>Msg</code> 中常见的消息类型有：<code>Warning</code>、<code>Debug</code>、<code>Error</code>、<code>Info</code>、<code>StatuBar</code>，其他例如窗口图标，则需要具体考虑了。</p>
<h3 id="cmake"><a class="markdownIt-Anchor" href="#cmake"></a> CMAKE</h3>
<p>添加了<code>I18n.h</code> 之后，要记得在对应的<code>.cpp</code> 文件中实现方法，并且在同级的<code>CMAKELIST.txt</code> 中检查是否将文件添加进去，否则可能造成编译时找不到定义的错误。</p>
]]></content>
      <categories>
        <category>cpp</category>
      </categories>
      <tags>
        <tag>gettext</tag>
        <tag>多语言</tag>
        <tag>国际化</tag>
      </tags>
  </entry>
</search>
